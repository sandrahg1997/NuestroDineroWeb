import AppShell from"@/components/AppShell";import PageHeader from"@/components/PageHeader";import CategoryManager from"@/components/CategoryManager";import{getSessionContext}from"@/lib/data";import{redirect}from"next/navigation";export default async function Page(){const{supabase,user,householdId,households,hideAmounts}=await getSessionContext();if(!user)redirect("/login");if(!householdId)redirect("/settings");const{data}=await supabase.from("categories").select("*").eq("household_id",householdId).order("type").order("name");return <AppShell households={households} hideAmounts={hideAmounts}><PageHeader title="Categorías" subtitle="Crea categorías propias para gastos e ingresos."/><CategoryManager householdId={householdId} initial={(data??[])as any}/></AppShell>}
